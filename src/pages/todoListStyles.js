const TodoComponent = {
    maxWidth: 1024,
    minHeight: '100vh',
    backgroundColor: '#9ef08c',
    padding: 20,
}
const nthChildColor = {
    backgroundColor: '#7d7726',
}
const nthChildColorOdd = {
    backgroundColor: '#447d26',
}
const padding = {
    paddingBottom: 5,
}

const completed = {
    textDecoration: 'line-through'
}
const noCompleted = {
    textDecoration: 'none'
}
const textShadow = {
    textShadow: '8px 8px 1px rgba(147, 149, 150, 1)'
}
export const styles = {
    TodoComponent,
    padding,
    completed,
    noCompleted,
    textShadow,
    nthChildColor,
    nthChildColorOdd
}